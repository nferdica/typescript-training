let statsAtual: unknown = 1;

let mudaStats: number = 0;

mudaStats = statsAtual as number;

console.log(mudaStats);

let query: unknown = 'Pizza'

let searchQuery: string = query as string;

console.log(searchQuery);